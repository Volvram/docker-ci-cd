{
  "name": "node-mongo-docker",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "build": "echo 'Building...' && mkdir -p dist && cp -r src/* dist/",
    "build-win": "echo Building... && mkdir dist 2>nul && xcopy /s /y src\\* dist\\",
    "prod": "node dist/index.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "mongoose": "^8.16.4"
  }
}
